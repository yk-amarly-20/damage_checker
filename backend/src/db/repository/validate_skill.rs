use crate::domain::models::skill::skill::Skill;

// paramをskill型にconvertする
// TODO: ファイル命名考える
pub struct ValidateSkillRepository {}

/*
impl ValidateSkillRepository {
    pub fn validate(skill_name: &'static str) -> Skill {
        // 技情報が書き込まれているファイル名を取得
        // 正規表現で検索
        fn get_file_name() -> &'static str {

        }
        let first_char = skill_name.chars().next().unwrap();

    }
}
*/
